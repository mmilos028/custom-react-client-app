import { Theme, createStyles } from '@material-ui/core';


const CreateMemberCardPageStyle = (theme: Theme) => createStyles({
    content: {
        width: 600
    }
});

export default CreateMemberCardPageStyle;