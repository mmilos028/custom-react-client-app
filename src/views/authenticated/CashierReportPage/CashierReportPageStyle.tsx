import { Theme, createStyles } from '@material-ui/core';


const CashierReportPageStyle = (theme: Theme) => createStyles({
    content: {
        width: 600
    }
});

export default CashierReportPageStyle;