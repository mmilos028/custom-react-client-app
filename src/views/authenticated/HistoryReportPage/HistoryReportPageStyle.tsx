import { Theme, createStyles } from '@material-ui/core';


const HistoryReportPageStyle = (theme: Theme) => createStyles({
    content: {
        width: 600
    }
});

export default HistoryReportPageStyle;