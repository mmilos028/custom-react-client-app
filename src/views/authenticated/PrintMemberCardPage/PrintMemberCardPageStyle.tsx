import { Theme, createStyles } from '@material-ui/core';

const PrintMemberCardPageStyle = (theme: Theme) => createStyles({
    content: {
        minWidth: 600,
        width: '100%'
    }
});

export default PrintMemberCardPageStyle;